# Agent Instructions

Before doing any repository work, read `docs/AGENTS.md`.

The instructions in `docs/AGENTS.md` are authoritative for this project. If local tooling appears missing, check that file before treating verification as skipped.
